package com.example.cashpilot.ui.theme

import androidx.compose.ui.graphics.Color

val DarkGray = Color(0xFF202020)     // Темно-серый (фон экрана)
val LightGray = Color(0xFF292929)    // Светло-серый (карточки, контейнеры)
val GreenB = Color(0xFF53B63A)        // Зеленый (акцент, кнопки)
val LightBlack = Color(0xFF1A1A1A)   // Светло-черный (альтернативный фон)
val Red = Color(0xFFE53935)          // Красный (ошибки, предупреждения)
val WhiteTransparent = Color.White.copy(alpha = 0.1f) // Полупрозрачный белый (границы)